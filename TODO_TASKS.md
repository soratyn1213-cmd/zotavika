# TODO: Список задач для студентов

## ✅ Быстрый старт

1. Откройте `ЗАДАНИЕ_ДЛЯ_СТУДЕНТОВ.md` для подробного описания
2. Найдите TODO комментарии в коде:
   ```bash
   grep -r "TODO:" src/
   ```
3. Реализуйте методы согласно заданию

## 📝 Задачи (по приоритету)

### Задача 1: Лайки постов (10 баллов)

**Файлы для редактирования:**
- `src/main/java/com/myblog/controller/PostController.java`
  - Метод `addLike()` - строка ~86
  - Метод `removeLike()` - строка ~98
  
- `src/main/java/com/myblog/service/impl/PostServiceImpl.java`
  - Метод `addLike()` - добавить новый
  - Метод `removeLike()` - добавить новый
  
- `src/main/java/com/myblog/dao/impl/PostDaoImpl.java`
  - Метод `incrementLikes()` - добавить новый
  - Метод `decrementLikes()` - добавить новый

**Пример SQL для incrementLikes:**
```sql
UPDATE posts SET likes_count = likes_count + 1 WHERE id = ?
```

### Задача 2: Управление комментариями (10 баллов)

**Файлы для редактирования:**
- `src/main/java/com/myblog/controller/CommentController.java`
  - Метод `updateComment()` - строка ~50
  - Метод `deleteComment()` - строка ~62
  
- `src/main/java/com/myblog/service/impl/CommentServiceImpl.java`
  - Метод `updateComment()` - добавить новый
  - Метод `deleteComment()` - добавить новый
  
- `src/main/java/com/myblog/dao/impl/CommentDaoImpl.java`
  - Метод `update()` - добавить новый
  - Метод `delete()` - добавить новый

**Пример SQL для update:**
```sql
UPDATE comments SET text = ?, updated_at = NOW() WHERE id = ? AND post_id = ?
```

### Задача 3: Удаление поста (5 баллов)

**Файлы для редактирования:**
- `src/main/java/com/myblog/controller/PostController.java`
  - Метод `deletePost()` - строка ~79
  
- `src/main/java/com/myblog/service/impl/PostServiceImpl.java`
  - Метод `deletePost()` - добавить новый
  
- `src/main/java/com/myblog/dao/impl/PostDaoImpl.java`
  - Метод `delete()` - добавить новый

**Важно:** Используйте `@Transactional` для каскадного удаления!

**Порядок удаления:**
1. Удалить все комментарии поста
2. Удалить все связи с тегами (post_tags)
3. Удалить сам пост

## 🧪 Тестирование

После реализации каждого метода:

1. **Компиляция:**
   ```bash
   mvn clean compile
   ```

2. **Запуск тестов:**
   ```bash
   mvn test
   ```

3. **Ручное тестирование через curl:**
   ```bash
   # Добавить лайк
   curl -X POST http://localhost:8080/api/posts/1/likes
   
   # Удалить лайк
   curl -X DELETE http://localhost:8080/api/posts/1/likes
   
   # Обновить комментарий
   curl -X PUT http://localhost:8080/api/posts/1/comments/1 \
     -H "Content-Type: application/json" \
     -d '{"id":1,"text":"Новый текст","postId":1}'
   
   # Удалить комментарий
   curl -X DELETE http://localhost:8080/api/posts/1/comments/1
   
   # Удалить пост
   curl -X DELETE http://localhost:8080/api/posts/1
   ```

## 📚 Полезные советы

1. **Посмотрите на существующий код** - методы `createPost`, `updatePost`, `addComment` уже реализованы и могут служить примером
2. **Используйте логирование** - добавляйте `log.debug()` для отладки
3. **Обрабатывайте ошибки** - возвращайте правильные HTTP статусы
4. **Пишите тесты** - это поможет убедиться в правильности работы

## 🎯 Как сдавать

1. Убедитесь, что все TODO выполнены
2. Проверьте, что проект компилируется: `mvn clean package`
3. Запустите тесты: `mvn test`
4. Создайте README.md с описанием выполненной работы
5. Загрузите в Git репозиторий

Удачи! 🚀


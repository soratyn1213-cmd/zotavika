# Задание для студентов: Доработка блога

## 📋 Описание проекта

Вам предоставлен частично реализованный backend для блога на Spring Framework 6.1+ (без Spring Boot). 
Ваша задача — **дописать недостающий функционал** согласно требованиям.

## 📦 Репозиторий проекта

**GitHub:** https://github.com/Yandex-Practicum/mcrpo-my-blog-pre

## ✅ Что уже реализовано

- ✅ Получение списка постов с пагинацией и поиском
- ✅ Получение поста по ID
- ✅ Создание нового поста
- ✅ Редактирование поста
- ✅ Загрузка и получение изображения поста
- ✅ Получение списка комментариев
- ✅ Получение комментария по ID
- ✅ Добавление комментария
- ✅ CORS фильтр
- ✅ Обработка ошибок (GlobalExceptionHandler)

## ❌ Что нужно реализовать (ваше задание)

### 1. Лайки постов (10 баллов)

#### 1.1 Добавление лайка к посту
**Endpoint:** `POST /api/posts/{id}/like`

**Требования:**
- Увеличивает счётчик `likesCount` для поста с ID = `{id}`
- Возвращает статус `200 OK` при успехе
- Возвращает статус `404 Not Found`, если пост не найден

**Где писать:**
- Controller: `src/main/java/com/myblog/controller/PostController.java` (метод `addLike`)
- Service: `src/main/java/com/myblog/service/impl/PostServiceImpl.java` (метод `addLike`)
- DAO: `src/main/java/com/myblog/dao/impl/PostDaoImpl.java` (метод `incrementLikes`)

**Подсказка:** Посмотрите на метод `createPost` как пример работы с базой данных.

#### 1.2 Удаление лайка у поста
**Endpoint:** `DELETE /api/posts/{id}/like`

**Требования:**
- Уменьшает счётчик `likesCount` для поста с ID = `{id}`
- Не может уменьшить ниже 0
- Возвращает статус `200 OK` при успехе
- Возвращает статус `404 Not Found`, если пост не найден

**Где писать:**
- Controller: `src/main/java/com/myblog/controller/PostController.java` (метод `removeLike`)
- Service: `src/main/java/com/myblog/service/impl/PostServiceImpl.java` (метод `removeLike`)
- DAO: `src/main/java/com/myblog/dao/impl/PostDaoImpl.java` (метод `decrementLikes`)

### 2. Управление комментариями (10 баллов)

#### 2.1 Редактирование комментария
**Endpoint:** `PUT /api/posts/{postId}/comments/{id}`

**Тело запроса:**
```json
{
  "id": 2,
  "text": "Обновлённый текст комментария",
  "postId": 1
}
```

**Ответ:**
```json
{
  "id": 2,
  "text": "Обновлённый текст комментария",
  "postId": 1
}
```

**Требования:**
- Обновляет текст комментария с ID = `{id}` для поста `{postId}`
- Обновляет поле `updatedAt` в базе данных
- Возвращает обновлённый комментарий
- Возвращает статус `404 Not Found`, если комментарий не найден

**Где писать:**
- Controller: `src/main/java/com/myblog/controller/CommentController.java` (метод `updateComment`)
- Service: `src/main/java/com/myblog/service/impl/CommentServiceImpl.java` (метод `updateComment`)
- DAO: `src/main/java/com/myblog/dao/impl/CommentDaoImpl.java` (метод `update`)

**Подсказка:** Посмотрите на `PostController.updatePost` как пример.

#### 2.2 Удаление комментария
**Endpoint:** `DELETE /api/posts/{postId}/comments/{id}`

**Требования:**
- Удаляет комментарий с ID = `{id}` для поста `{postId}`
- Уменьшает `commentsCount` поста на 1
- Возвращает статус `200 OK` при успехе
- Возвращает статус `404 Not Found`, если комментарий не найден

**Где писать:**
- Controller: `src/main/java/com/myblog/controller/CommentController.java` (метод `deleteComment`)
- Service: `src/main/java/com/myblog/service/impl/CommentServiceImpl.java` (метод `deleteComment`)
- DAO: `src/main/java/com/myblog/dao/impl/CommentDaoImpl.java` (метод `delete`)

### 3. Удаление поста (5 баллов)

**Endpoint:** `DELETE /api/posts/{id}`

**Требования:**
- Удаляет пост с ID = `{id}`
- Удаляет все комментарии к этому посту
- Удаляет все связи с тегами (таблица `post_tags`)
- Возвращает статус `200 OK` при успехе
- Возвращает статус `404 Not Found`, если пост не найден

**Где писать:**
- Controller: `src/main/java/com/myblog/controller/PostController.java` (метод `deletePost`)
- Service: `src/main/java/com/myblog/service/impl/PostServiceImpl.java` (метод `deletePost`)
- DAO: `src/main/java/com/myblog/dao/impl/PostDaoImpl.java` (метод `delete`)

**Важно:** Удаление должно быть **транзакционным** (используйте `@Transactional`)

## 📝 Тесты (бонус +15 баллов)

Напишите **unit-тесты** для всех новых методов:
- Тесты для Service слоя (используя Mockito)
- Тесты для Controller слоя (используя MockMvc)
- Тесты для DAO слоя (используя встроенную H2)

**Примеры тестов** уже есть в проекте в папке `src/test/java/com/myblog/`

## 🛠 Как начать работу

### 1. Клонирование проекта

```bash
git clone https://github.com/Yandex-Practicum/mcrpo-my-blog-pre.git
cd mcrpo-my-blog-pre
```

### 2. Установка зависимостей и сборка

```bash
mvn clean install
```

### 3. Найдите TODO комментарии

Все места, где нужно дописать код, помечены комментариями `TODO`:

```bash
grep -r "TODO:" src/
```

Или просмотрите файл `TODO_TASKS.md` в корне проекта.

### 4. Реализуйте методы согласно заданию

Откройте проект в IntelliJ IDEA или другой IDE и начните реализацию.

### 5. Запустите тесты

```bash
mvn test
```

### 6. Соберите WAR файл

```bash
mvn clean package
```

WAR файл появится в `target/my-blog-back-app.war`

### 7. Разверните на Tomcat/Jetty

**Вариант 1 - Jetty (встроенный, рекомендуется):**
```bash
mvn jetty:run -Dmaven.test.skip=true
```

Backend запустится на http://localhost:8080

**Вариант 2 - Tomcat:**

```bash
# 1. Скопируйте WAR в webapps (замените /path/to/tomcat на ваш путь):
cp target/my-blog-back-app.war /path/to/tomcat/webapps/ROOT.war

# 2. Запустите Tomcat через скрипты в папке bin:
/path/to/tomcat/bin/startup.sh

# Остановка Tomcat (если нужно):
/path/to/tomcat/bin/shutdown.sh

# Для macOS с Homebrew:
brew services start tomcat
```

**Примеры путей:**
- macOS (Homebrew): `/opt/homebrew/opt/tomcat/libexec/`
- Linux: `/opt/tomcat/` или `/usr/local/tomcat/`
- Windows: `C:\Program Files\Apache Tomcat\`

### 8. Запустите Frontend (опционально)

⚠️ **Требуется Node.js 18+ и npm**

Если не установлен:
- macOS: `brew install node`
- Windows: https://nodejs.org
- Linux: `sudo apt install nodejs npm`

```bash
cd frontend
npm install
npm run dev
```

Frontend запустится на http://localhost:3000

> **Примечание:** Frontend опционален - можно тестировать backend через curl/Postman.

## 📚 Полезные ресурсы

- [Spring Framework Documentation](https://docs.spring.io/spring-framework/docs/6.1.x/reference/html/)
- [Spring JDBC](https://docs.spring.io/spring-framework/docs/current/reference/html/data-access.html#jdbc)
- [JUnit 5 User Guide](https://junit.org/junit5/docs/current/user-guide/)
- [Mockito Documentation](https://javadoc.io/doc/org.mockito/mockito-core/latest/org/mockito/Mockito.html)

## 🎯 Критерии оценки

| Задача | Баллы | Требования |
|--------|-------|------------|
| Добавление лайка | 5 | Корректная работа endpoint, обновление БД |
| Удаление лайка | 5 | Корректная работа endpoint, обновление БД, проверка на 0 |
| Редактирование комментария | 5 | Корректная работа endpoint, обновление БД |
| Удаление комментария | 5 | Корректная работа endpoint, каскадное удаление |
| Удаление поста | 5 | Транзакционное удаление, каскадное удаление комментариев |
| Unit-тесты (бонус) | +15 | Покрытие тестами всех новых методов |
| **ИТОГО** | **25-40** | |

## ✅ Как сдавать

### Подготовка к сдаче

1. **Форкните репозиторий** https://github.com/Yandex-Practicum/mcrpo-my-blog-pre
2. **Клонируйте свой форк** на локальную машину
3. **Создайте отдельную ветку** для разработки:
   ```bash
   git checkout -b feature/student-implementation
   ```
4. **Реализуйте все задания** (см. выше)
5. **Убедитесь, что все тесты проходят:**
   ```bash
   mvn test
   ```
6. **Создайте WAR файл:**
   ```bash
   mvn clean package
   ```
7. **Протестируйте функционал** на работающем сервере (Jetty/Tomcat)

### Создание Pull Request

1. **Закоммитьте изменения:**
   ```bash
   git add .
   git commit -m "Реализовано задание: лайки, комментарии, удаление постов"
   ```

2. **Отправьте изменения в свой форк:**
   ```bash
   git push origin feature/student-implementation
   ```

3. **Создайте Pull Request** в оригинальный репозиторий:
   - Перейдите на страницу своего форка на GitHub
   - Нажмите "New Pull Request"
   - В заголовке укажите: `[ФИО] Выполнение задания`
   - В описании укажите:
     - Что было реализовано
     - Какие тесты написаны
     - Что работает, а что нет (если есть проблемы)

4. **Отправьте ссылку на Pull Request** преподавателю

### Критерии приёмки

- ✅ Все TODO выполнены
- ✅ Код компилируется без ошибок
- ✅ Все существующие тесты проходят
- ✅ WAR файл собирается
- ✅ Функционал работает на сервере
- ✅ (Бонус) Написаны новые тесты

## 🚀 Успехов в выполнении задания!


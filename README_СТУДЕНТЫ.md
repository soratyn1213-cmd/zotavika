# 📝 Практическая работа: REST API для блога

## 👨‍🎓 Для студентов

Это **учебный проект** для практики работы со Spring Framework. 

Вам предоставлен **частично реализованный** backend. Ваша задача — **дописать недостающий функционал**.

## 📦 Репозиторий проекта

**GitHub:** https://github.com/Yandex-Practicum/mcrpo-my-blog-pre

**Как начать работу:**

```bash
git clone https://github.com/Yandex-Practicum/mcrpo-my-blog-pre.git
cd mcrpo-my-blog-pre
mvn clean install
```

## 🎯 Ваша задача

**Реализовать 3 группы методов:**

1. ✅ **Лайки постов** (добавление/удаление) - 10 баллов
2. ✅ **Управление комментариями** (редактирование/удаление) - 10 баллов  
3. ✅ **Удаление постов** (с каскадным удалением) - 5 баллов

**Бонус:** Написать тесты для всех методов - +15 баллов

## 📚 С чего начать

### Шаг 1: Изучите задание

Откройте файл `ЗАДАНИЕ_ДЛЯ_СТУДЕНТОВ.md` - там подробное описание каждого метода.

### Шаг 2: Найдите TODO

```bash
grep -r "TODO:" src/
```

Вы увидите список всех мест, где нужно дописать код.

### Шаг 3: Посмотрите примеры

Изучите уже реализованные методы:
- `PostController.createPost()` - пример создания поста
- `PostController.updatePost()` - пример обновления
- `CommentController.createComment()` - пример работы с комментариями

### Шаг 4: Реализуйте методы

Начните с простого - например, с добавления лайка:

1. Откройте `PostController.java` 
2. Найдите метод `addLike()`
3. Реализуйте логику согласно TODO комментарию
4. Реализуйте соответствующий метод в `PostService`
5. Реализуйте SQL запрос в `PostDao`

### Шаг 5: Тестируйте

После каждого метода:

```bash
# Компиляция
mvn clean compile

# Сборка WAR
mvn clean package -Dmaven.test.skip=true

# Деплой на Tomcat и тестирование
```

## 🛠 Технологии

- **Spring Framework 6.1+** (без Spring Boot!)
- **Java 17**
- **Maven** - сборка проекта
- **H2 Database** - in-memory база данных
- **JUnit 5 + Mockito** - тестирование
- **Tomcat 11** - сервер приложений

## 📂 Структура проекта

```
mcrpo-my-blog-pre/
├── src/main/java/
│   └── com/myblog/
│       ├── config/           # Конфигурация Spring
│       ├── controller/       # REST контроллеры
│       ├── service/          # Бизнес-логика
│       ├── dao/              # Работа с БД
│       ├── model/            # Модели данных
│       ├── dto/              # DTO для API
│       ├── filter/           # CORS фильтр
│       └── exception/        # Обработка ошибок
├── src/main/resources/
│   ├── schema.sql            # SQL схема БД
│   └── application.properties
├── src/test/java/            # Тесты
├── frontend/                 # React frontend (готовый)
├── pom.xml                   # Maven зависимости
├── ЗАДАНИЕ_ДЛЯ_СТУДЕНТОВ.md  # Подробное задание
└── TODO_TASKS.md             # Список TODO задач
```

## 📋 Что уже работает

✅ CRUD операции с постами (кроме DELETE)  
✅ Загрузка/получение изображений  
✅ CRUD операции с комментариями (кроме UPDATE и DELETE)  
✅ Поиск и пагинация постов  
✅ Работа с тегами  
✅ CORS для работы с фронтендом  
✅ Обработка ошибок (JSON вместо HTML)

## ❌ Что нужно дописать (ваше задание)

- [ ] POST /api/posts/{id}/likes - добавление лайка
- [ ] DELETE /api/posts/{id}/likes - удаление лайка
- [ ] PUT /api/posts/{postId}/comments/{id} - редактирование комментария
- [ ] DELETE /api/posts/{postId}/comments/{id} - удаление комментария
- [ ] DELETE /api/posts/{id} - удаление поста

## 🚀 Запуск проекта

### 1. Клонирование и установка зависимостей

```bash
git clone https://github.com/Yandex-Practicum/mcrpo-my-blog-pre.git
cd mcrpo-my-blog-pre
mvn clean install
```

### 2. Сборка WAR файла

```bash
mvn clean package -Dmaven.test.skip=true
```

WAR файл появится в `target/my-blog-back-app.war`

### 3. Запуск backend

**Вариант А - Jetty (встроенный, рекомендуется):**

```bash
mvn jetty:run -Dmaven.test.skip=true
```

Backend запустится на http://localhost:8080

**Вариант Б - Tomcat:**

```bash
# 1. Скопируйте WAR файл в директорию webapps вашего Tomcat
#    Замените /path/to/tomcat на реальный путь к Tomcat
cp target/my-blog-back-app.war /path/to/tomcat/webapps/ROOT.war

# 2. Запустите/перезапустите Tomcat через скрипты в папке bin:

# Остановка (если запущен):
/path/to/tomcat/bin/shutdown.sh

# Запуск:
/path/to/tomcat/bin/startup.sh

# Или для macOS с Homebrew:
brew services restart tomcat
```

**Примеры путей к Tomcat:**
- macOS (Homebrew): `/opt/homebrew/opt/tomcat/libexec/`
- Linux: `/opt/tomcat/` или `/usr/local/tomcat/`
- Windows: `C:\Program Files\Apache Tomcat\`

### 4. Запуск frontend (опционально)

⚠️ **Требуется Node.js 18+ и npm**

Если у вас не установлен Node.js:
- **macOS**: `brew install node`
- **Windows**: Скачайте с https://nodejs.org
- **Linux**: `sudo apt install nodejs npm` или `sudo yum install nodejs npm`

```bash
cd frontend
npm install
npm run dev
```

Frontend запустится на http://localhost:3000

> **Примечание:** Frontend не обязателен для тестирования backend. Вы можете использовать curl или Postman.

### 5. Проверка

Откройте: http://localhost:8080/api/posts?search=&pageNumber=1&pageSize=10

Если видите JSON ответ - backend работает! ✅

## 🧪 Тестирование

### Ручное тестирование

Используйте curl или Postman:

```bash
# Создать пост
curl -X POST http://localhost:8080/api/posts \
  -H "Content-Type: application/json" \
  -d '{"title":"Тест","text":"Текст поста","tags":["тест"]}'

# Добавить лайк (после реализации)
curl -X POST http://localhost:8080/api/posts/1/likes

# Получить пост
curl http://localhost:8080/api/posts/1
```

### Автоматические тесты

```bash
mvn test
```

## 📖 Полезные ресурсы

- [Spring Framework Docs](https://docs.spring.io/spring-framework/docs/6.1.x/reference/html/)
- [Spring JDBC](https://docs.spring.io/spring-framework/docs/current/reference/html/data-access.html#jdbc)
- [JUnit 5 Guide](https://junit.org/junit5/docs/current/user-guide/)
- [HTTP Status Codes](https://httpstatuses.com/)

## 💡 Советы

1. **Начните с простого** - сначала реализуйте addLike, потом остальное
2. **Используйте существующий код** как примеры
3. **Пишите логи** для отладки: `log.debug("...", param)`
4. **Тестируйте после каждого метода** - не пишите всё сразу
5. **Обращайте внимание на HTTP статусы** - 200 OK, 404 Not Found и т.д.

## ❓ FAQ

**Q: Где писать код?**  
A: Ищите комментарии `// TODO:` в коде

**Q: Как проверить, что всё работает?**  
A: 1) Компиляция без ошибок 2) Тесты проходят 3) API отвечает правильно

**Q: Можно ли менять уже написанный код?**  
A: Лучше не надо - только добавляйте новое

**Q: Как работать с БД?**  
A: Смотрите примеры в `PostDaoImpl` - используйте `JdbcTemplate`

## 📬 Сдача работы

### Процесс сдачи:

1. **Форкните репозиторий** https://github.com/Yandex-Practicum/mcrpo-my-blog-pre
2. **Реализуйте все методы** из задания
3. **Убедитесь что всё работает:**
   ```bash
   mvn clean package
   mvn test
   ```
4. **Создайте Pull Request** в оригинальный репозиторий
5. **Отправьте ссылку** на Pull Request преподавателю

### Что должно быть в Pull Request:

- ✅ Заголовок: `[Ваше ФИО] Выполнение задания`
- ✅ Описание: что реализовано, какие тесты написаны
- ✅ Все TODO выполнены
- ✅ Код компилируется без ошибок
- ✅ Тесты проходят

**Подробная инструкция:** см. `ЗАДАНИЕ_ДЛЯ_СТУДЕНТОВ.md`

---

## 🎓 Удачи в выполнении задания!

Если возникнут вопросы - смотрите файлы:
- `ЗАДАНИЕ_ДЛЯ_СТУДЕНТОВ.md` - подробное описание задач
- `TODO_TASKS.md` - краткий чеклист
- Существующий код - примеры реализации

**Помните:** Вы учитесь не только писать код, но и читать чужой код, понимать архитектуру и работать с реальными технологиями! 💪


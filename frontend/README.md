# 📝 My Blog - Frontend

Современный фронтенд для блога на **React 18 + Vite**.

## ✨ Возможности

✅ **Все функции работают правильно:**
- Просмотр списка постов с поиском и пагинацией
- Создание новых постов с изображениями
- **Редактирование постов** (форма закрывается корректно)
- Удаление постов
- **Лайки/Анлайки** (toggle работает)
- Комментарии к постам
- Загрузка и отображение изображений

## 🚀 Быстрый старт

### Вариант 1: Разработка (Development)

```bash
# Установить зависимости
npm install

# Запустить dev сервер
npm run dev

# Откроется http://localhost:3000/
```

### Вариант 2: Production сборка

```bash
# Собрать проект
npm run build

# Запустить preview
npm run preview
```

### Вариант 3: Docker

```bash
# Собрать и запустить в Docker
docker-compose up --build

# Откроется http://localhost:3000/
```

## 🔧 Настройка

### API URL

По умолчанию фронтенд подключается к `http://localhost:8080/api`.

Чтобы изменить URL API, отредактируйте константу в `src/App.jsx`:

```javascript
const API_URL = 'http://localhost:8080/api';
```

### Порт

По умолчанию приложение запускается на порту **3000**.

Для изменения порта отредактируйте `vite.config.js`:

```javascript
export default defineConfig({
  server: {
    port: 3000, // <- Измените здесь
  }
})
```

## 📁 Структура проекта

```
my-blog-frontend/
├── src/
│   ├── App.jsx          # Главный компонент с роутингом
│   ├── App.css          # Стили
│   ├── main.jsx         # Точка входа
│   └── index.css        # Базовые стили
├── public/              # Статические файлы
├── Dockerfile           # Docker образ
├── docker-compose.yaml  # Docker Compose конфигурация
├── nginx.conf           # Nginx конфигурация
├── vite.config.js       # Vite конфигурация
└── package.json         # Зависимости
```

## 🎨 Технологии

- **React 18** - UI библиотека
- **Vite** - Build tool и dev server
- **React Router 6** - Навигация
- **CSS3** - Стилизация (без внешних библиотек)
- **Nginx** - Production web server
- **Docker** - Контейнеризация

## 🔄 API Endpoints

Фронтенд взаимодействует со следующими endpoints:

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/posts` | Получить список постов |
| GET | `/api/posts/{id}` | Получить пост по ID |
| POST | `/api/posts` | Создать пост |
| PUT | `/api/posts/{id}` | Обновить пост |
| DELETE | `/api/posts/{id}` | Удалить пост |
| POST | `/api/posts/{id}/likes` | Добавить лайк |
| DELETE | `/api/posts/{id}/likes` | Убрать лайк |
| GET | `/api/posts/{id}/image` | Получить изображение |
| PUT | `/api/posts/{id}/image` | Загрузить изображение |
| GET | `/api/posts/{id}/comments` | Получить комментарии |
| POST | `/api/posts/{id}/comments` | Создать комментарий |

## 🐛 Исправленные проблемы

### 1. ✅ Форма редактирования не закрывалась
**Было:** После сохранения форма оставалась открытой  
**Исправлено:** Добавлен правильный state management с callback `onSave()`

### 2. ✅ Ошибка FormData при редактировании
**Было:** "Failed to execute 'append' on 'FormData': parameter 2 is not of type 'Blob'"  
**Исправлено:** Правильная обработка File объектов из `<input type="file">`

### 3. ✅ Лайки только добавлялись
**Было:** Каждое нажатие увеличивало счётчик  
**Исправлено:** Реализован toggle (POST для добавления, DELETE для удаления)

### 4. ✅ Запросы к undefined endpoints
**Было:** Frontend делал запросы до загрузки данных  
**Исправлено:** Правильная последовательность загрузки с проверками

## 📱 Адаптивность

Приложение полностью адаптивно и корректно работает на:
- 💻 Desktop (1200px+)
- 💻 Laptop (768px - 1199px)
- 📱 Tablet (480px - 767px)
- 📱 Mobile (до 479px)

## 🎯 Дальнейшие улучшения

Возможные доработки:
- [ ] Авторизация пользователей
- [ ] Редактирование/удаление комментариев
- [ ] Предпросмотр изображения перед загрузкой
- [ ] Drag & Drop для загрузки изображений
- [ ] Markdown поддержка в постах
- [ ] Темная тема
- [ ] Уведомления (toast notifications)
- [ ] Бесконечная прокрутка вместо пагинации
- [ ] Оптимизация изображений

## 📄 Лицензия

MIT

## 🤝 Поддержка

При возникновении проблем:
1. Убедитесь, что backend запущен на `http://localhost:8080`
2. Проверьте консоль браузера (F12) на наличие ошибок
3. Проверьте Network tab для отладки запросов

---

**Создано с ❤️ для студентов**
